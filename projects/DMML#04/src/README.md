# Source Code & Implementation 💻

This directory contains the core Python implementation for the SLM-driven log anomaly detection pipeline.

## 🛠️ Installation
Before running the script, install the required dependencies using the `requirements.txt` file:

```bash
pip install -r requirements.txt
